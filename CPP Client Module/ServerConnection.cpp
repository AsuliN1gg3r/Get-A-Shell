#include "ServerConnection.h"

const bool ServerConnection::run(void)
{
    try
    {
        while (true)
        {

        }
    }
    catch (...)
    {
        return false;
    }
    return true;
}
